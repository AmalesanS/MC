spring:
 application:
  name: loan-auth-service
 cloud:
  config:
   uri: http://localhost:8888
 profiles:
  active: native
server:
 port: 8085
eureka:
 client:
  service:
   url:
    default-zone: http://localhost:8761

